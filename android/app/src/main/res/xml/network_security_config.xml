<?xml version="1.0" encoding="utf-8"?>
<!--
    Allow cleartext (HTTP) traffic to localhost and private/LAN IP ranges so that
    the app can connect to user-hosted local AI servers such as Ollama
    (default http://localhost:11434) and LocalAI (default http://localhost:8080)
    running on the device, in an emulator, or on the same local network.
    All other (public) traffic remains HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <!-- Android emulator host loopback -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">10.0.3.2</domain>
        <!-- Private LAN ranges (RFC1918) -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <!-- mDNS / Bonjour hosts -->
        <domain includeSubdomains="true">local</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
