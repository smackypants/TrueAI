<!--
  Thanks for contributing to TrueAI LocalAI! Please fill in the
  sections below. The "Lessons learned" section is parsed by
  .github/workflows/learnings-ingest.yml when this PR is merged into
  main, and is auto-appended to .github/copilot/LEARNINGS.md so
  future agent / Copilot sessions can read it on every task.

  Keep "Lessons learned" actionable: hard constraints, gotchas,
  conventions a future agent would benefit from knowing. One to
  three bullets is ideal. If your change makes a previous learning
  obsolete, prefix the bullet with `SUPERSEDES: <old title>`.
-->

## Summary

<!-- What does this PR do, and why? -->

## Changes

<!-- Bullet list of the concrete file / behaviour changes. -->

## Validation

<!-- How was this tested? Commands run, results, screenshots. -->

- [ ] `npm ci` succeeds
- [ ] `npm run build:dev` succeeds
- [ ] `npm run lint` succeeds
- [ ] `npm test` succeeds (if applicable)
- [ ] `Analyze (javascript-typescript)`, `Analyze (java-kotlin)`, and
      `Android CI` status checks pass

## Lessons learned

<!--
  The contents of this section are auto-appended to
  .github/copilot/LEARNINGS.md on merge. Replace this comment with
  one to three bullets capturing non-obvious decisions, conventions,
  or gotchas. Leave the section empty if there are none — the ingest
  workflow will fall back to a stub entry.
-->
